The django_tutorial name of the folder is the name of the project, the main folder in my site is one application, just 1 part of a project.
Can have more than 1 application inside project
For templating you need a templates folder, and within that folder you need a folder that same name as your app

Folder structure
templates
.. main

How do forms work?

just pass a {{form}} variable, Django autogenerates the form

